import { TitleCasePipePipe } from './title-case-pipe.pipe';

describe('TitleCasePipePipe', () => {
  it('create an instance', () => {
    const pipe = new TitleCasePipePipe();
    expect(pipe).toBeTruthy();
  });
});
